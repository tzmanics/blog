<h1>Articles Tagged with <%= @tag.name%></h1>

<ul>
	<% @tag.articles.each do |article| %>
		<l1><%= link_to article.title, article_path(article) %></li><br />
	<% end %>
</ul>
<%= link_to "<< back to article list", articles_path %>